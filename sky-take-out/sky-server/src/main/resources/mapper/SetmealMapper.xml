<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealMapper">
<!--Ctrl+Alt+L格式化xml-->
<!--  TODO:这里虽然有自动填充字段-但是也需要写    </trim>如果用if判断为否则不添加列 -->
<!--    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into setmeal(category_id, name, price, status, description, image)
        values
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="categoryId!=null">
                category_id=#{categoryId},
            </if>
            <if test="name!=null">
                name=#{name},
            </if>
            <if test="price!=null">
                price=#{price},
            </if>
            <if test="status!=null">
                status=#{status},
            </if>
            <if test="description!=null">
                description=#{description},
            </if>
            <if test="image!=null">
                image=#{image},
            </if>
        </trim>
        </insert>
    -->
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into setmeal
        (category_id, name, price, status, description, image, create_time, update_time, create_user, update_user)
        values (#{categoryId}, #{name}, #{price}, #{status}, #{description}, #{image}, #{createTime}, #{updateTime},
                #{createUser}, #{updateUser})
    </insert>

    <select id="list" parameterType="Setmeal" resultType="Setmeal">
        select * from setmeal
        <where>
            <if test="name != null">
                and name like concat('%',#{name},'%')
            </if>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>



    <select id="page" resultType="com.sky.vo.SetmealVO">
        select a.*,b.name as categoryName from setmeal a left outer join category b on a.category_id=b.id
        <where>
            <if test="name!=null">
                and a.name like concat('%',#{name},'%')
            </if>
            <if test="categoryId!=null">
                and a.category_id like concat('%',#{categoryId},'%')
            </if>
            <if test="status!=null">
                and a.status like concat('%',#{status},'%')
            </if>
        </where>
        order by a.create_time DESC
    </select>


    <select id="countByMap" resultType="java.lang.Integer">
        select count(id) from setmeal
        <where>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
        </where>
    </select>
</mapper>
